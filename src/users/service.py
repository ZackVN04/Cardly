from src.users.schemas import UserPublic, UserSearchList

# Implementation: Phase 3 — W5 (Khanh)
# get_public_profile(user_id: str) -> UserPublic
# search_users(q: str, page: int, limit: int) -> UserSearchList
