from src.scans.schemas import ExtractedData

# Implementation: Phase 3 — W6 (Huy)
# extract_card_data(image_url) -> ExtractedData   — Vertex AI / HF model
# parse_ocr_response(raw_response) -> ExtractedData
# mock_extract() -> ExtractedData                 — used when ENVIRONMENT='test'
