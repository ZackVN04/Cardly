# Implementation: Phase 3 — W7 (Huy)
# extract_card_data(image_url: str) -> dict
#   Calls Gemini Vision API via httpx with the image URL
#   Returns extracted fields: full_name, company, title, email, phone, website, address
