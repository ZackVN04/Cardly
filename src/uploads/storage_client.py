# Implementation: Phase 3 — W6 (Huy)
# upload_to_gcs(file_bytes: bytes, destination_path: str, content_type: str) -> str
#   Uses google-cloud-storage to upload bytes to GCP_BUCKET; returns public URL
# delete_from_gcs(path: str) -> None
# get_public_url(path: str) -> str
