from src.contacts.schemas import ContactCreate, ContactUpdate

# Implementation: Phase 3 — W6 (Khanh)
# create_contact(owner_id: str, data: ContactCreate) -> ContactDoc
# list_contacts(owner_id: str, page: int, limit: int, q: str | None, tag_id: str | None, event_id: str | None, sort_by: str) -> ContactListResponse
# get_contact(contact_id: str, owner_id: str) -> ContactDoc
# update_contact(contact_id: str, owner_id: str, data: ContactUpdate) -> ContactDoc
# delete_contact(contact_id: str, owner_id: str) -> None
# add_tag(contact_id: str, owner_id: str, tag_id: str) -> ContactDoc
# remove_tag(contact_id: str, owner_id: str, tag_id: str) -> ContactDoc
