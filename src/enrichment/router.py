from fastapi import APIRouter

router = APIRouter(prefix="/enrichment", tags=["enrichment"])

# Implementation: Phase 3 — W6–W7 (Khanh)
# GET /  · POST /{contactId}  · GET /{contactId}  · PATCH /{contactId}  · DELETE /{contactId}
