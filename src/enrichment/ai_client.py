from src.enrichment.schemas import EnrichmentResponse

# Implementation: Phase 3 — W7 (Khanh)
# fetch_linkedin_data(linkedin_url) -> dict | None
# fetch_website_data(website_url) -> dict | None
# fetch_facebook_data(facebook_url) -> dict | None
# call_gemini(contact_data, social_data) -> dict
# parse_enrichment_result(gemini_response) -> dict
# mock_enrich(contact_id) -> dict   — used when ENVIRONMENT='test'
