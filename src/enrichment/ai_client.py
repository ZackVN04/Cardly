# Implementation: Phase 3 — W7 (Huy)
# enrich_contact(contact_data: dict) -> dict
#   Calls Gemini API via httpx with contact fields
#   Returns enriched fields: linkedin_url, twitter_url, bio, company_info, etc.
