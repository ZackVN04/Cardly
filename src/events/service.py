from src.events.schemas import EventCreate, EventUpdate

# Implementation: Phase 3 — W4 (Khanh)
# create_event(owner_id: str, data: EventCreate) -> EventDoc
# list_events(owner_id: str, page: int, limit: int) -> EventListResponse
# get_event(event_id: str, owner_id: str) -> EventDoc
# update_event(event_id: str, owner_id: str, data: EventUpdate) -> EventDoc
# delete_event(event_id: str, owner_id: str) -> None
